#python code to demonstartae working of
#truediv(), floordiv(), pow(), mod()

import operator
#initializing variable
a=5
b=3
#using truediv to divide two numbers
print (The true division of two numbers : ", end="");
print (operator.truediv(a,b))

#using floordiv to divide two numbers
print ("The floor division of two numbers : ", end="");
print (operator.floordiv(a,b))

#using pow() to exponentiate two numbers
print ("The exponentiation of two numbers : ", end="");
print (operator.pow(a,b))

#using mod() to take modulus of two numbers
print ("The modulus of two numbers : ", end="");
print (operator.mod(a,b))

